A simple demo rails application of capybara, polymorphic associations, and google maps.
setup:
  - normal rails setup (bundle install, rake db:migrate, etc)
  - Please change the google maps api key if you fork. It will break in the future.
testing:
  - run 'rspec && rspec spec/features/*'
